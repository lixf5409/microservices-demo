
server:
  port: 8762

spring:
  application:
    name: service-user
  profiles:
    active: dev
  cloud:
    consul:
      #consul地址
      host: localhost
      #consul 对外提供服务的端口
      port: 8501
      discovery:
        #服务提供者对外提供的健康检查接口，默认为/actuator/health
#        healthCheckPath: /actuator/health

feign:
  hystrix:
    enabled: true

#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka/

mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml
  config-location:  classpath:/mybatis/config/mybatis-config.xml
